---
import Invite from "@/components/invite.astro";
import Layout from "@/layout/base.astro";
import {
  ariNumber,
  confirmMessage,
  createWhatsappLink,
} from "@/utils/whatsapp";
---

<Layout>
  <Invite url={createWhatsappLink(ariNumber, confirmMessage)} />
</Layout>
